# SUPERSTACK

Смотрит, как настроен твой Claude Code, объясняет каждый вывод и без спроса ничего не меняет.

## Установка

### Не открывая терминал

Вставьте этот текст своему агенту — Claude Code, Cursor, Codex. Он всё сделает сам:

```
Поставь мне SUPERSTACK. Выполни в терминале ровно эти команды, по одной:

CLAUDECODE= claude plugin marketplace add edvardgrishin27/superstack-plugin
CLAUDECODE= claude plugin install superstack-core@superstack
CLAUDECODE= claude plugin install superstack-install@superstack
CLAUDECODE= claude plugin install superstack-spec@superstack
CLAUDECODE= claude plugin install superstack-guard@superstack
CLAUDECODE= claude plugin install superstack-build@superstack
CLAUDECODE= claude plugin install superstack-brain@superstack
CLAUDECODE= claude plugin install superstack-control@superstack

Префикс `CLAUDECODE=` обязателен: без него `claude` откажется запускаться
изнутри сессии («cannot be launched inside another Claude Code session»),
и это не поломка — это защита от вложенных сессий.

Ставь все семь. Поставить часть нельзя: движок пока не тянет зависимости между
пакетами, а инструменты зовут друг друга через пакеты. Неполный набор даёт
рабочий на вид `/go`, который падает на первом же гейте.

Больше ничего не устанавливай. Когда закончишь — напиши одной строкой, что
готово, и напомни перезапустить сессию: плагины читаются при старте.
```

### Из терминала

```bash
claude plugin marketplace add edvardgrishin27/superstack-plugin
for p in core install spec guard build brain control; do
  claude plugin install "superstack-$p@superstack"
done
```

Здесь префикс не нужен — вы уже вне сессии.

**Слэш-команда `/plugin` не подойдёт.** Она есть только в терминальном Claude
Code; в десктопном приложении её нет, и попытка выполнить её там отвечает
«isn't available in this environment». Проверено на десктопе, не выведено.

Дальше он заговорит сам — печатать команды не нужно.

**Что здесь проверено, а что выведено.** Проверено запуском: приватность
репозитория (`gh repo view`), отказ анонимного доступа (`curl` к API → `404`,
`git clone` → `Repository not found`), работа `git ls-remote` при живом доступе.
Выведено, а не проверено: поведение самой `/plugin marketplace add` у человека
без доступа — выполнить её от чужого лица со стороны владельца невозможно, вывод
сделан из того, что источник маркетплейса — тот же недоступный репозиторий.

---

<details>
<summary>Для тех, кто хочет знать, что именно он делает</summary>

**Четыре фазы, в этом порядке.** Профиль — детерминированные пробы читают конфиги,
считают скиллы и хуки, проверяют память и MCP; ничего не меняется. Вердикт — правила
из `plugins/superstack/rules/*.json` применяются к фактам, отчёт открывается страницей.
Применение — сначала бэкап, удаление запрещено (всё уходит в карантин и возвращается
одной командой). Доказательство — сквозной прогон, где падающий тест подкладывает сама
система, чтобы зелёный результат нельзя было нарисовать.

**Три правила, которые не нарушаются.**

1. Спрашивать человека только о том, что он знает из своей жизни. Всё измеримое —
   измеряется скриптом. «Сколько у тебя скиллов» — не вопрос, а проба.
2. Не утверждать того, чего не измерил. У каждого факта есть происхождение:
   измерено · выведено · неоднозначно. Если проба не отработала, отчёт помечается
   недостоверным целиком — «не нашёл» и «не смог проверить» это разные утверждения.
3. Ничего не удалять. Карантин вместо `rm`, бэкап до первой правки, секреты не
   копируются даже в бэкап.

**Проверить руками.** `python3 tools/render.py findings.json why <id>` печатает
правило-источник, буквальные значения фактов и команду, которой это перепроверяется.
Верить на слово не обязательно.

**Остановить.** `SUPERSTACK_DISABLE=1` в окружении гасит все хуки. `tools/pause.sh on`
ставит файл-флаг, который проверяется первым действием каждой итерации — тормоз
работает, даже когда агент не отвечает.

Требования: `python3` из системы, без единой внешней зависимости. Тесты: `pytest`.

</details>
