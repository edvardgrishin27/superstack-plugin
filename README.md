# SUPERSTACK

Смотрит, как настроен твой Claude Code, объясняет каждый вывод и без спроса
ничего не меняет.

```
/plugin marketplace add edvardgrishin27/superstack-plugin
/plugin install superstack-core@superstack
/plugin install superstack-guard@superstack
/plugin install superstack-install@superstack
```

Три пакета — и ты уже защищён. Дальше он заговорит сам, печатать команды не нужно.

---

## Новичку

Ты ставишь и **ничего не печатаешь** — система заговорит при первой сессии
и спросит разрешения. Не более трёх раз за всё время; скажешь «нет» — больше
не спросит никогда.

Она открывает страницу: что нашла, почему это важно, что предлагает сделать.
Пароль в открытом виде, выключенные проверки, забитая память — покажет раньше,
чем ты об это споткнёшься.

**Ничего не трогает без спроса.** Копию настроек делает до первой правки,
ничего не удаляет — только откладывает, откуда возвращается одной командой.

**Четыре команды, больше знать не нужно:**

| | |
|---|---|
| `/go` | «хочу вот такое» → доводит до работающего куска |
| `/what` | где я и что дальше |
| `/oops` | отменить последнее |
| `/fix` | что-то сломалось |

**Главное.** Не даёт агенту сказать «готово», когда не готово: гейт запускает
проверки проекта и решает **кодом возврата**, а не формулировкой. И отдельно —
если тестов нет вовсе, это не «прошло», а «проверять нечем».

## Опытному

**Ничего не утверждается без измерения.** У каждого факта есть происхождение —
измерено · выведено · неоднозначно. Проба, которая не отработала, гасит доверие
ко **всему** отчёту: «не нашёл» и «не смог проверить» здесь разные утверждения,
и они не смешиваются даже в свою пользу.

**Готовность считает скрипт.** Шесть ворот, код возврата:

```bash
python3 tools/gauntlet.py     # 0 взята · 1 красное · 2 не проверено
```

| ворота | что доказывает |
|---|---|
| набор | тесты зелёные — слабейшие: зелёное можно нарисовать |
| **герметичность** | то же число тестов при другом `HOME` |
| **мутации** | 124 зарегистрированные поломки, каждая обязана уронить набор |
| правила | схема, дубли, имена фактов, подстановки |
| манифест | пакеты ставятся, а не только лежат |
| план | механизмы из карты на месте, улика ищется в **коде**, не в прозе |

Двое из них существуют потому, что остальные обманываются. Набор однажды уехал
с 260 на 282 теста **без единой правки кода** — читал настоящий `~/.claude`.
А внешние проверки дважды показывали: набор зелёный, но до 70% механизмов можно
удалить, и ни один тест не заметит.

**Всё обратимо.** Бэкап до первой правки, карантин вместо `rm`, секреты не
копируются даже в бэкап — на их месте отпечаток, и об этом говорят заранее.

## Семь пакетов

Ставь только нужное. Зависимости объявлены и подтянутся сами.

| пакет | что даёт | зависит от |
|---|---|---|
| `superstack-core` | пробы, решатель, отчёт страницей, журнал, тормоз | — |
| `superstack-guard` | гейт верификации, доказательство, второе мнение | core |
| `superstack-install` | применение с бэкапом и карантином, вход при первой сессии | core |
| `superstack-brain` | линт памяти, журнал находок, базовая линия, доктор | core |
| `superstack-spec` | линт спеки, граф артефактов | core |
| `superstack-build` | `/go` — стройка с двумя гейтами | spec, guard |
| `superstack-control` | `/what`, `/oops`, `/fix` | core, install, spec, guard |

## Проверить руками

```bash
cd plugins/superstack-core
python3 tools/probe/collect.py > facts.json
python3 tools/adjudicate.py facts.json 'rules/*.json' > findings.json
python3 tools/render.py findings.json why <id>
```

`why` печатает правило-источник, буквальные значения фактов и команду для
перепроверки. Верить на слово не требуется.

## Остановить

`SUPERSTACK_DISABLE=1` гасит все хуки. `pause.sh on` ставит файл-флаг, который
проверяется первым действием каждой итерации: тормоз работает, даже когда тот,
кого тормозят, не отвечает.

## Требования

Системный `python3` — проверено на 3.9.6. **Ни одной внешней зависимости.**
Тесты: `pytest`.

730 тестов · 124 мутации, все ловятся · 58 механизмов. MIT.
