#!/bin/sh
# SUPERSTACK — тормоз. Тонкая обёртка над pause.py.
#
# Сам тормоз переехал на Python: аварийная остановка обязана работать ВЕЗДЕ,
# а скрипт оболочки не запускается там, где оболочки нет — то есть отказывает
# ровно в тот момент, когда всё уже идёт не так.
#
# Обёртка осталась, потому что у людей эта команда в пальцах и в старых
# записях. Двух реализаций при этом нет: здесь одна строка.
set -u
exec python3 "$(dirname "$0")/pause.py" "$@"
