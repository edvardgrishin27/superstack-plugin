#!/bin/sh
# SUPERSTACK — первое предложение. Тонкая обёртка над first-run.py.
#
# Хук переехал на Python. Прежняя версия возражала: «ни python3, ни jq не
# гарантированы в момент старта сессии». Аргумент пересмотрен — на машине без
# python3 SUPERSTACK не работает вообще, и звать туда человека хуже молчания.
set -u
exec python3 "$(dirname "$0")/first-run.py" "$@"
