#!/bin/sh
# SUPERSTACK — страховка памяти. Тонкая обёртка над precompact.py.
#
# Хук переехал на Python: `sh` был лишней зависимостью, а разбор JSON на sed
# спотыкался об экранированную кавычку внутри значения — это было записано в
# скрипте как осознанный пропуск. Порт убрал и то, и другое.
set -u
exec python3 "$(dirname "$0")/precompact.py" "$@"
