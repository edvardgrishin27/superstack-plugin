#!/bin/sh
# SUPERSTACK — вопрос об уроке. Тонкая обёртка над session-lesson.py.
#
# Сам хук переехал на Python: `sh` был ЛИШНЕЙ зависимостью — питон продукту
# нужен в любом случае, на нём все инструменты, а оболочка нужна была только
# хукам. Обёртка осталась для тех, у кого путь к .sh записан в настройках.
set -u
exec python3 "$(dirname "$0")/session-lesson.py" "$@"
