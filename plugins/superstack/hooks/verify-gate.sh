#!/bin/sh
# SUPERSTACK — гейт верификации. Тонкая обёртка над verify-gate.py.
#
# Хук переехал на Python: `sh` был лишней зависимостью, а вместе с ним ушла
# самая хрупкая часть — экранирование JSON руками через tr/sed/awk. Кавычка
# или табуляция в выводе проверки ломали разбор всего объекта, и хук переставал
# существовать для Claude Code целиком, молча.
set -u
exec python3 "$(dirname "$0")/verify-gate.py" "$@"
